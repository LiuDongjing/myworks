#include <iostream>
#include <vector>
#include <ctime>
using namespace std;
int partition(vector<int> &data, int start, int end) {

}
int main() {
	srand(time(0));
	int cases;
	cin >> cases;
	for (; cases > 0; cases--) {
		int n;
		cin >> n;
		vector<int> data(n);
		for (int i = 0; i < n; i++)
			cin >> data[i];
		
	}

}